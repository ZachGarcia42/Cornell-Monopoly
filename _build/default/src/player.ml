open Monopoly

type player = {
  name: string;
}

let money_left pl= raise (Failure "Not implemented")
let has_enough_money pl bd= raise (Failure "Not implemented")

let is_bankrupt money = raise (Failure "Not implemented")


